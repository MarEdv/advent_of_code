# This YAML file describes your package. Stack will automatically generate a
# Cabal file when you run `stack build`. See the hpack website for help with
# this file: <https://github.com/sol/hpack>.
category: Other
description: day20 is a new Haskeleton package.
executables:
  day20:
    dependencies:
    - base
    - split
    - range
    ghc-options:
    - -rtsopts
    - -threaded
    - -with-rtsopts=-N
    main: Main.hs
    source-dirs: executable
extra-source-files:
- CHANGELOG.md
- LICENSE.md
- package.yaml
- README.md
- stack.yaml
ghc-options: -Wall
github: maredv/day20
license: MIT
maintainer: Marcus Edvinsson
name: day20
synopsis: A new Haskeleton package.
version: '0.0.0'
